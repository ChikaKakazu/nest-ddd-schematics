{
    "$schema": "http://json-schema.org/schema",
    "$id": "Resource",
    "title": "DDD Resource Schema",
    "type": "object",
    "properties": {
        "name": {
        "type": "string",
        "description": "The name of the resource.",
        "$default": {
            "$source": "argv",
            "index": 0
        }
        }
    },
    "required": ["name"]
}
